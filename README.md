## Task 1 - configure and document the repository
 - The port shouldn't be hardcoded here, this configuration should come from a config file or a environment variable. 
         
## Task 2 - Improve Performance
 - Since I don’t have control over the endpoint’s implementation, my initial approach was to issue multiple concurrent requests. While there may be optimizations possible in the code, the primary concern lies in the current request handling strategy.
